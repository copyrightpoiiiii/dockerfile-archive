# syntax=docker/dockerfile:1.4
FROM test:baseImage 
RUN apt-get -y install curl && apt-get clean && rm -rf /var/lib/apt/lists/* && rm -rf /var/log/* && rm -rf /var/cache/* && rm -rf /usr/share/doc/*
RUN curl -SsO https://dl.yarnpkg.com/debian/pubkey.gpg 
# COPY test.txt /test.txt