command:
  providerFor:
    - cd
  prefix: SC-CD
  scenarios:
    - cmd: $0 <path>
      options: 
        booleans:
          - -L
          - -P
      name: SC-CD
      replaceEmptyArgsWith: [ '~' ]